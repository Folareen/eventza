{
  "name": "eventza",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "dev:api": "turbo run dev --filter=@eventza/api",
    "dev:web": "turbo run dev --filter=@eventza/web"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}